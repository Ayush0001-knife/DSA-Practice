#include <iostream>
using namespace std;

int main()
{
      vector<vector<char>> board = {
          {'A', 'B', 'C', 'E'},
          {'S', 'F', 'C', 'S'},
          {'A', 'D', 'E', 'E'}};

      int rows = board.size();
      int cols = board[0].size();

      string word = "ABCCED";
      string newWord = "";
      int count = 0;

      for (int i = 0; i < rows; i++)
      {
            for (int j = 0; j < cols; j++)
            {
            }
      }

      return 0;
}